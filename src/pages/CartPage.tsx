import CartTable from "../components/CartTable";
import './CartPage.css';

export default function CartPage() {
    return (
        <div className="cart-page">
            <CartTable/>
        </div>
    );
}